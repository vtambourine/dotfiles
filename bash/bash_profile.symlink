export RBENV_ROOT=/usr/local/rbenv
eval "$(rbenv init -)"

DOTFILES_DIR=$HOME/Code/dotfiles

declare -ra source_files=(
  $DOTFILES_DIR/bash/bash_prompt
  $DOTFILES_DIR/bash/options
)


for file in ${source_files[@]}; do
  [ -f $file ] && source $file
done
# . ./bash_prompt
